import unittest
import tui/app

test "TUI runs":
  # This is a very basic test to ensure the TUI can be initialized and closed.
  # More advanced testing of the TUI would require a mock terminal or a different approach.
  discard
